{
  "name": "to-do-list",
  "version": "1.0.0",
  "description": "lista no node",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build" : 'npm run build',
    "publicar": "npm run build && ./node_modules/.bin/gh-pages -d build"
  },
  "author": "Ivan",
  "license": "ISC",
  "dependencies": {
    "ejs": "^3.1.8",
    "express": "^4.18.1",
    "method-override": "^3.0.0",
    "mongoose": "^6.3.3"
  },
  "devDependencies": {
    "nodemon": "^2.0.16"
  }
}
